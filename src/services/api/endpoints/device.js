import { WALLET_ENDPOINT_V1 } from "@constants/url";

const DEVICE_LIST_BY_USERNAME = `${WALLET_ENDPOINT_V1}/device/listByUsername`;
const DEVICE_ADD = `${WALLET_ENDPOINT_V1}/device/add`;

export { DEVICE_ADD, DEVICE_LIST_BY_USERNAME };
