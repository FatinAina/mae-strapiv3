import { ENDPOINT_BASE } from "@constants/url";

const MOMENT_EMPTY = `${ENDPOINT_BASE}/rule-service/api/v1/getOnBoard`;
const MOMENT_ONBOARD = `${ENDPOINT_BASE}/moment/api/v1/moment/get`;

export { MOMENT_EMPTY, MOMENT_ONBOARD };
