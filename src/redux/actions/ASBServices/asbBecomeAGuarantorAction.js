export const ASB_BECOME_GUARANTOR_CONSENT_LOADING = () => ({});

export const ASB_BECOME_GUARANTOR_ERROR = (error) => ({
    error,
});

export const ASB_BECOME_GUARANTOR_SUCCESS = (data) => ({
    data,
});

export const ASB_BECOME_GUARANTOR_CLEAR = () => ({});
