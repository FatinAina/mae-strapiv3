export const AUTHORISE_FPX_TRANSACTION_LOADING = () => ({});

export const AUTHORISE_FPX_TRANSACTION_ERROR = (error) => ({
    error: error,
});

export const AUTHORISE_FPX_TRANSACTION_SUCCESS = (data) => ({
    data: data,
});

export const AUTHORISE_FPX_TRANSACTION_CLEAR = () => ({});
