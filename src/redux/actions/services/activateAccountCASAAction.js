export const ACTIVATE_ACCOUNT_CASA_LOADING = () => ({});

export const ACTIVATE_ACCOUNT_CASA_ERROR = (error) => ({
    error: error,
});

export const ACTIVATE_ACCOUNT_CASA_SUCCESS = (data) => ({
    data: data,
});

export const ACTIVATE_ACCOUNT_CASA_CLEAR = () => ({});

export const ZEST_CASA_ACTIVATE_ACCOUNT_CASA_BODY = (state) => ({
    state,
});
