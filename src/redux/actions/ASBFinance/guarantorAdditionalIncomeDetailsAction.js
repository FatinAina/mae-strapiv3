export const GUARANTOR_FIXED_ALLOWANCE = (fixedAllowance) => ({
    fixedAllowance,
});
export const GUARANTOR_NON_FIXED_AALOWANCE = (nonFixedAllowance) => ({
    nonFixedAllowance,
});
export const GUARANTOR_EPF_DIVIDEND = (epfDividend) => ({
    epfDividend,
});
export const GUARANTOR_COMMISSION = (commission) => ({
    commission,
});
export const GUARANTOR_HOUSEHOLD_INCOME = (householdIncome) => ({
    householdIncome,
});
export const GUARANTOR_BONUS_TYPE = (bonusType) => ({
    bonusType,
});
export const GUARANTOR_ANNUAL_BONUS_AMOUNT = (annualBonusAmount) => ({
    annualBonusAmount,
});
export const GUARANTOR_TOTAL_AMOUNT = (totalAmount) => ({
    totalAmount,
});
export const GUARANTOR_ADDITIONAL_INCOME_DEATAIL_SUBMIT_BUTTON_ENABLE = () => ({});

export const GUARANTOR_ADDITIONAL_INCOME_DETAILS_CLEAR = () => ({});
