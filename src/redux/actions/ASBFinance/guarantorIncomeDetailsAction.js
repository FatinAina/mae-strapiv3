export const GUARANTOR_MONTHLY_GROSS_INCOME = (monthlyGrossInformation) => ({
    monthlyGrossInformation,
});

export const GUARANTOR_TOTAL_MONTHLY_NON_BANK_COMMITMENTS = (
    total_monthly_non_banking_commitments
) => ({
    total_monthly_non_banking_commitments,
});

export const GUARANTOR_INCOME_DETAILS_NEXT_BUTTON_ENABLE = (incomeDetailsNextBottonEnable) => ({
    incomeDetailsNextBottonEnable,
});

export const GUARANTOR_INCOME_DETAILS_CLEAR = () => ({});
